# Dependencies & build artifacts — installed/built inside the image
**/node_modules
**/dist
**/.next
**/.turbo
**/coverage

# Env & secrets (the .env.example is kept for reference)
.env
.env.*
!.env.example

# VCS & editor cruft
.git
.gitignore
**/.DS_Store

# Not needed inside the build context
docs
*.log
